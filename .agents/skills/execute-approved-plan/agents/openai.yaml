interface:
  display_name: "Execute Approved Plan"
  short_description: "Safely execute an approved repository plan"
  default_prompt: "Use $execute-approved-plan to execute the approved plan at the path I provide."

policy:
  allow_implicit_invocation: false
