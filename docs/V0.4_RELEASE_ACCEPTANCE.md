# v0.4 Release Acceptance — Competition Closure

> Package checkpoint: `v0.4.0`
>
> Competition runtime: `v0.4.5`
>
> Metric protocol: `v045_competition_metric_protocol_v2_existing_gold_only`
>
> Target end state: `v0.4.5 COMPETITION_READY`
>
> Current verdict: **NOT YET COMPETITION_READY**

本文件是当前比赛 Gate 与 blocker 的唯一状态源。指标定义以 `COMPETITION_METRIC_PROTOCOL.md` 为准；当前操作层顺序以 `V045_CURRENT_EXECUTION_PLAN.md` 为准。

## 1. 当前执行快照

2026-08-27 Role-B 已进入 constrained Lunamax/Codex Runner 模式，不再给 Codex 开放式“全仓优化”任务。

最近一次本地 fixed-10 真实运行已经完成：

```text
real-LLM analysis = 10/10 completed
Validation opened = false
2025 Blind accessed = false
```

随后在 Existing-Gold evaluator handoff 阶段暴露新的 serialization blocker：

```text
EXECUTION_BLOCKED
ValueError: governed result missing case_id
```

根因已定位为 runner 汇总 `analysis_results.jsonl` 时没有显式携带外层已知的 canonical `case_id`。该缺陷不属于 LLM、Retriever、Gold 或 evaluator metric 定义问题。

当前实现已经修复：

```text
1. governed JSONL row 始终写入 canonical case_id；
2. metadata.case_id / top-level case_id 如已存在必须与 expected case_id 一致；
3. 冲突时 fail closed，不静默覆盖；
4. 不修改原始 analysis_result.json；
5. evaluator contract 不放宽。
```

为了保护已经完成的 10/10 real-LLM 结果，新增纯离线恢复入口：

```bash
python scripts/recover_v045_role_b_iteration.py --iteration <existing_iteration_id>
```

该恢复路径只读取既有 persisted results，重建 governed JSONL、重新运行 Existing-Gold evaluator，并生成 `iteration_summary.json` / `failure_focus.json`；不重新进入 real-LLM analysis runtime。

当前恢复约束：

```text
external_llm_calls_added = 0
Validation = false
2025 Blind = false
```

因此，当前立即动作不是重新跑 10 家，而是：

```text
pull latest main
-> identify the existing completed iteration id
-> run recover_v045_role_b_iteration.py
-> materialize M1/M2/Recall@K + failure_focus
-> stop
```

M1/M2 在本地 recovery summary 真正生成前继续保持 OPEN，不做数值宣称。

## 2. 已关闭 Gate / tooling

| Gate | Status | Current evidence |
|---|---|---|
| Public competition runtime contracts | PASS | `competition_runtime_v1` sidecar 已进入代码与 CI |
| Remote LLM provider boundary | PASS | `openai_responses` structured path 已由 1167.HK 真实 PDF 全流程验证 |
| Legal / Business LLM implementation | PASS implementation | real provider path 已被真实案例触发并 harden |
| Governed MarketContext | PASS | PIT governed，missingness 不补零 |
| Market Intelligence | PASS implementation | IPOHeatSkill / MarketRegimeSkill / bounded Market LLM |
| Conflict / bounded re-check | PASS implementation | typed + auditable + bounded |
| Agent / Tool / Evidence trace | PASS implementation | measured，不硬编码 |
| Human Review / Streamlit | PASS implementation | 五工作区可运行 |
| 3 real prospectus offline E2E | PASS | 2410 / 2460 / 1318 completed |
| 3-case PDF integrity | PASS | SHA-256 + size + physical pages = 3/3 |
| 3-case offline traceability | PASS = 1.0 | 三案均 1.0 |
| Single-case real-provider smoke | PASS | 1167.HK completed；Final Supervisor accepted；no fallback；scope PASS |
| Submission artifacts/tooling | PASS implementation | readiness / audits / artifact index / packager 已实现 |
| Existing Expert Gold inventory | FROZEN | 101 inventory / 100 valid / 98 official materialized |
| Metric Protocol v2 Existing-Gold-Only | **FROZEN** | no new annotation / no UNJUDGED-as-negative |
| Existing-Gold coverage audit / evaluator | **PASS** | 79 Development / 19 Validation；128 Risk Units；217 Evidence Units |
| Fixed-10 Development iteration tooling | **PASS implementation** | deterministic subset + manifest binding + resume + evaluator + compact failure focus |
| Governed-result case identity serialization | **FIXED** | canonical `case_id` is injected at JSONL handoff; conflicts fail closed |
| Fixed-10 offline recovery tooling | **PASS implementation** | persisted results -> evaluator -> summary/failure focus with zero new LLM calls |
| Lunamax/Codex constrained Runner protocol | **PASS operating procedure** | Runner/Fixer scope remains bounded |
| 2025 Blind protection | PASS for current measured runs | 当前实测均未访问 |

“PASS implementation / operating procedure” 不代表最终真实 metric handoff 已关闭。

## 3. Metric-v2 正式比赛口径

### M1 Risk Extraction

```text
Existing-Gold Official-aligned Accuracy
= correct evaluable positive Existing-Gold Risk Units
  / all evaluable positive Existing-Gold Risk Units

Official pass >=0.80
Project target >=0.85
```

`UNJUDGED` 不当 negative、不进入分母。必须披露 evaluable support、correct count、per-risk support/correct/recall。

### M2 Evidence

```text
Existing-Gold Evidence Coverage Recall
= covered evaluable existing Evidence Units
  / all evaluable existing Evidence Units

Official pass >=0.85
Project target >=0.88
```

Recall@1/@3/@5/@10/@20 只作 diagnostics；不新增 Evidence Gold，不人工重新做 semantic Evidence Group。

### M3 / M4 / M5

```text
M3 Traceability =1.0
M4 Explanation Quality = current final product rubric
M5 1D/5D/20D/60D complete
Primary significant_drop_5d = return_5d <= -0.10
```

## 4. Existing-Gold coverage Gate B0 — PASS

```text
manifest_hash = fcd12d34fcc64853ed778d0026b1e2c943a863549cd1652c6ced7c6145214d1c
official_existing_gold_case_count = 98
evaluable_development_case_count = 79
evaluable_validation_case_count = 19
primary_positive_risk_unit_count = 128
primary_evidence_unit_count = 217
```

Primary support：

```text
cash_burn_pressure         16
customer_concentration     32
redemption_rights          39
supplier_concentration     41
related_party_transaction   0 -> NOT_EVALUABLE_FROM_EXISTING_GOLD
```

治理：

```text
new_manual_annotations_added = false
existing_gold_modified = false
blind_2025_outcome_accessed = false
```

## 5. Gate B1 — M1：OPEN / P0

历史旧 offline diagnostic 不是 metric-v2 正式证据：

```text
Risk Precision  0.0%
Risk Recall     0.0%
Risk F1         0.0%
Real LLM cases  0
```

当前 fixed-10 已完成 10/10 real-LLM analysis，但 formal Metric-v2 summary 尚待离线 recovery 物化。因此当前 closure 路线改为：

```text
recover existing fixed-10 persisted results
-> materialize baseline M1/M2/Recall@K
-> 2-4 rounds maximum targeted Development iteration
-> larger Development checkpoint
-> ALL 79 Development real-LLM benchmark
-> freeze
-> one-shot ALL 19 Validation
```

关闭 B1 需要：

```text
ALL 79 Development
metric_protocol_version = v045_competition_metric_protocol_v2_existing_gold_only
official_aligned_accuracy >=0.80
per-risk support disclosed
new_manual_annotations_added=false
existing_gold_modified=false
Blind=false
```

内部目标 Accuracy >=0.85。

## 6. Gate B2 — M2：OPEN / P0

历史 offline diagnostic：

```text
Evidence Recall@1 = 20%
Evidence Recall@3 = 20%
Evidence Recall@5 = 20%
Physical-page correctness = 100%
```

这些不等于 M2 primary。

关闭 B2：

```text
Existing-Gold Evidence Coverage Recall >=0.85 official
Project target >=0.88
```

同时报告：

```text
Candidate Retrieval Recall@20
Reranked Recall@10
Recall@1/@3/@5/@10/@20
```

## 7. fixed-10 当前口径

Metric-v2 fixed-10 唯一权威来源：

```text
reports/v045_role_b/fixed10_development_subset.json
```

不存在时：

```bash
python scripts/run_v045_role_b_iteration.py --subset-only
```

正常新一轮：

```bash
python scripts/run_v045_role_b_iteration.py --iteration auto
```

已经完成 real-LLM 但在 evaluator/summary 阶段失败的既有 iteration：

```bash
python scripts/recover_v045_role_b_iteration.py --iteration <existing_iteration_id>
```

恢复脚本必须：

```text
- 仅使用 persisted analysis_result.json；
- 10/10 result 缺失则 fail closed；
- subset hash 不一致则 fail closed；
- Validation/Blind governance flag 不合法则拒绝；
- external_llm_calls_added = 0。
```

历史 smoke 参考 10 家：

```text
1167.HK 加科思─B
1942.HK MOG Holdings
1961.HK 九尊数字互娱
9600.HK 新纽科技
9633.HK 农夫山泉
9898.HK 微博─SW
6698.HK 星空华文
9863.HK 零跑汽车
2451.HK 绿源集团控股
2517.HK 锅圈
```

该列表只用于 smoke/人工核对；不覆盖自动生成的正式 fixed-10。

详细公司表、Runner/Fixer/recovery 操作见：

```text
docs/V045_ROLE_B_LUNAMAX_AUTOMATION_RUNBOOK.md
docs/V045_CURRENT_EXECUTION_PLAN.md
```

## 8. Gate D1 — M5：OPEN / P0

必须物化：

```text
return_1d
return_5d
return_20d
return_60d

test_predictions.csv
multi_horizon_results.csv
evaluation_summary.json
ai_vs_offline_report.json
```

Primary：

```text
significant_drop_5d = (return_5d <= -0.10)
```

报告 Precision / Recall / F1 / PR-AUC / ROC-AUC / Top-10% / Top-20% hit rate / base prevalence。赛题没有绝对 5D 合格线，不因 Validation 结果反转/重训 frozen PR-F。

## 9. Gate C1 — Final-matrix Market validation：OPEN / P1

最终 3-case run 需证明 explicit market state + governed trace accounting + no fabricated market numbers。真实 missing 合法，zero/proxy 伪补不合法。

## 10. Gate E1 — Real-provider Final Supervisor：OPEN / P1

1167.HK 单案例 real-provider smoke 已 PASS，但 final matrix Gate 仍未关闭。

最终必须：

```text
2410.HK / 2460.HK / 1318.HK
real provider
accepted
complete call trace
scope PASS
severity floor respected
3/3
```

## 11. Gate E2 — Explanation Quality：OPEN / P1

沿用现有 explanation-quality 方案。本次 Existing-Gold-only M1/M2 不新增任何 E2 Gold 标注任务。

## 12. Gate A1 — final readiness：EXECUTION OPEN / P1

最终需完成：

- B/C/D/E metric-v2 handoff；
- latest-main full CI；
- final 3-case AI smoke；
- Blind / provenance / determinism actual PASS；
- final metric dashboard / artifact completeness；
- artifact index / release note / submission archive / security audit；
- 全部 hard Gate PASS 后才标记 `COMPETITION_READY`。

## 13. 当前明确不做

- 任何新的 M1/M2 人工 Gold；
- Gold 扩样/补 negative/人工重组 Evidence；
- PIT-safe industry-return 缺失时的历史补数据研究；
- ComparableIPOSkill；
- broad model search / hyperparameter sweep；
- full 438-case LLM run；
- 大规模 Retriever 重构；
- PR-F 替代训练；
- presentation-only UI expansion；
- 无 PIT 证据的 market proxy。

## 14. 五人当前状态

| Role | Current status | Closure responsibility |
|---|---|---|
| A | metric governance + submission tooling | final readiness / integration |
| B | **P0 quality blocker; 10/10 analysis done, metric recovery pending** | recover baseline -> targeted iteration -> ALL 79 M1/M2 |
| C | implementation largely closed | final-matrix governed Market trace |
| D | **P0 delivery/evaluation blocker** | M5 1D/5D/20D/60D + frozen 5D metrics |
| E | implementation largely closed | final 3-case E1 real-provider + M4 |

## 15. 最终 checklist

```text
[x] competition runtime contracts
[x] Market implementation / wiring
[x] conflict / re-check / Trace / Human Review / Streamlit
[x] 3 real PDF offline E2E
[x] 3-case offline traceability =1.0
[x] A submission tooling
[x] Existing Expert Gold inventory frozen
[x] Metric Protocol v2 Existing-Gold-Only frozen
[x] Existing-Gold coverage audit / manifest generated
[x] no new manual annotation / existing Gold unchanged proven
[x] single-case real-provider runtime smoke PASS
[x] fixed-10 Development iteration tooling implemented
[x] constrained Lunamax/Codex Runner procedure validated locally
[x] fixed-10 real-LLM analysis completed 10/10
[x] governed result case_id serialization fixed
[x] offline score recovery tooling implemented

[ ] current persisted fixed-10 recovery summary produced
[ ] fixed-10 M1/M2/Recall@K baseline materialized
[ ] B ALL 79 Development benchmark produced
[ ] M1 Existing-Gold Accuracy >=80%
[ ] M2 Existing-Gold Evidence Coverage Recall >=85%
[ ] Recall@K diagnostics produced
[ ] C final Market trace PASS
[ ] D multi-horizon artifacts produced
[ ] E real-provider 3/3 accepted
[ ] E current explanation quality PASS
[ ] final real traceability =1.0
[ ] final main CI green
[ ] blind / provenance / determinism PASS
[ ] artifact index / submission bundle / security audit PASS
[ ] release / COMPETITION_READY freeze complete
```
