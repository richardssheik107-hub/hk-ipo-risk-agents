# Release Acceptance — Competition Closure

> Competition runtime：`v0.4.5`  
> Role-B diagnostic track：`v0.4.6`  
> Metric protocol：`v045_competition_metric_protocol_v2_existing_gold_only`  
> 状态日期：`2026-08-29`  
> 当前结论：**NOT COMPETITION_READY**

本文档是当前**唯一实时 Release Gate**。研发优先级和五线并行策略见 `COMPETITION_CLOSURE_PLAN.md`。

## 0. Release policy

```text
current-case stage completed / UI green
!=
competition release gate passed
```

只有已经合入 main、经过相应 validator / benchmark / CI 的事实才能进入本文件的正式 PASS 状态。

Human Review UI/export 保留为 optional 产品能力；不要求 6 份真人 review，不是当前 Release Gate。

## 1. 当前已关闭稳定基线

```text
Final Supervisor E1 = 3/3
real-provider first-attempt accepted = 3/3
scope corrections = 0
fallback = 0
severity floor preserved = 3/3
M3 traceability = 1.0 x 3
Market final-three = 3/3 available
Frozen Model final-three = 3/3 available
targeted re-check = 17/17 attempted
budget-skipped = 0
seven-stage = 21/21
Evidence screenshots = 17/17 precise
canonical replay = 3 cases / 66 files
bundle hash verification = PASS
fresh clone / Streamlit smoke / team-ready checks = PASS
```

这些项进入 regression-protection 状态。

## 2. Role-B 正式 Release 状态

PR #189 已把 Batch008/009 accepted production fixes 与 fixed-journal checkpoint 合入 main。

最新可比 fixed-journal gated：

```text
Batch005  M1 = 12/30   M2 = 18/48
Batch008  M1 = 13/30   M2 = 20/48
Batch009  M1 = 14/30   M2 = 21/48
```

当前 Batch009：

```text
fixed-journal gated M1 = 14/30 = 46.67%
fixed-journal gated M2 = 21/48 = 43.75%
offline M1 = 9/30
offline M2 = 15/48
```

最后一个真实 fresh-provider checkpoint 仍是 Batch005：

```text
fresh gated M1 = 11/30 = 36.67%
fresh gated M2 = 17/48 = 35.42%
structured valid = 38/40
fallback = 2
transport failures = 0
scope rejections = 0
```

Batch008/009 使用 immutable local journal、`network_calls=0`，所以 **`14/30,21/48` 不得写成 fresh-provider 结果**。

已接受：

- Batch008 legacy Chinese cash statement / explicit Notes-column deterministic compatibility；
- Batch009 generalized Legal redemption/restoration lifecycle recognition，redemption-rights M1 `4/8 → 5/8`。

已拒绝并完整回滚：direct ranked concentration-table candidate；canonical M1/M2 无提升，supplier existence F1 `0.875 → 0.80`。不得直接恢复。

正式 G2 条件仍是：

```text
ALL79 Development
M1 >= 0.80 (target >=0.85)
M2 >= 0.85 (target >=0.88)
real_llm_cases = 79/79
new_manual_annotations_added = false
existing_gold_modified = false
Validation = false during optimization
Blind input/outcome not used for optimization
```

当前 root 顺序：

```text
retrieval candidate generation / ranking
→ exact page / anchor Evidence binding
→ remaining deterministic / numeric extraction
→ genuine conflict fail-closed
→ fixed-vs-fresh LLM / Evidence variance
```

Role-B 可用 multi-root wide sprint 加速，但只处理 proven compatible roots；每个子修复独立、可 partial revert，最终以 ALL79 泛化与 fresh stability 为准。

## 3. Role-D 当前状态

Frozen PR-F：

```text
2024 Validation IPOs = 70
1D / 5D / 20D / 60D artifacts = materialized
receipt / validator / CI = PASS
Blind outcome accessed = false
Precision = 0.3333
Recall = 0.0435
F1 = 0.0769
PR-AUC = 0.3364
ROC-AUC = 0.4246
```

v2 candidate：

```text
Precision = 0.3529
Recall = 0.5217
F1 = 0.4211
PR-AUC = 0.3812
ROC-AUC = 0.4875
promotion = package complete, effective on A-owned merge
```

V2 由 Development expanding folds 选择，2024 只作冻结后一次评价；版本化 freeze、严格 receipt、四项正式结果和 final-three label-free handoff 已生成，A 合并 promotion PR 是晋升决议生效证据。生效前正式产品身份仍以 frozen PR-F receipt 为准，旧 frozen PR-F 完整保留。

## 4. Final Gate 总表

| Gate | 状态 | 最终关闭条件 |
|---|---|---|
| G0 Runtime / contracts | PASS | latest-main CI 持续绿色、无回归 |
| G1 Stable final-three baseline | PASS | E1/M3/Market/Model/Recheck/Screenshot/Replay 持续无回归 |
| G2 M1/M2 Document Intelligence | **ACTIVE / P0** | ALL79 M1 `>=0.80`、M2 `>=0.85`、real LLM 79/79 |
| G3 Dynamic Market-X | **ACTIVE / P0** | historical universe audited + fresh new-IPO PIT path 或正式 external-data boundary |
| G4 Dynamic Model / SHAP + D decision | **ACTIVE / P0** | A 合并 V2 promotion PR 使决议生效 + real dynamic inference + native SHAP |
| G5 Final Frontend / Product | **ACTIVE / P0/P1** | Demo/Historical/Fresh 模式与所有 channel truthfully answer-ready |
| G6 Capability demonstrations | ACTIVE / P1 | pipeline / embellishment / related-party / comparable valuation / API/UI 有真实可审计证明 |
| G7 Freeze / Validation / package | **OPEN / P1 → final P0** | freeze → one-shot Validation → audits → fresh clone → secure bundle |

## 5. Dynamic Market-X Gate

当前已有 438 个 governed frozen Market-X Core artifacts 和 final-three 3/3 baseline。

最终需要统一 resolver：

```text
validated cache / frozen artifact
or
governed dynamic PIT provider
→ Market-X builder
→ schema / identity / provenance / hash validation
→ MarketContext
```

fresh case 若合法数据不足，允许 `PARTIAL / UNAVAILABLE`，但必须给出真实原因；禁止 post-listing leakage、zero-fill、final-three 值复制和 unsourced proxy。

## 6. Dynamic Model / SHAP Gate

当前 final-three per-case handoff 不等于泛化模型 runtime。

最终要求：

```text
governed feature vector
+ final frozen model artifact/hash
→ runtime inference (no retraining)
→ uncalibrated_model_score
→ frozen alert policy
→ native SHAP / signed drivers
→ ModelSignal
```

必须完成 `PROMOTE_V2` 或 `RETAIN_FROZEN_PR_F` 明确决议。若 promote，创建新的 versioned model/feature/alert/receipt，不覆盖历史 PR-F 身份，不再根据 2024 Validation 调参。

V2 promotion package 已完成：code/config/七特征/47.5% alert policy 已冻结；70-case 四项 Role-D artifact 已重建且 strict checker 独立复算 0 blocker；resume 与 fresh-directory 已验证 byte-identical；2410 / 2460 / 1318 label-free D→E package 已生成；性能、基准、34-alert 工作量和局限已进入正式报告。A 完成 promotion PR 审核并合并后，决议正式生效。

## 7. Final Frontend Gate

最终 UI 要清楚支持：

```text
Offline Demo Replay
Historical Governed IPO
Fresh New-IPO Analysis
```

并真实展示 Risk/Evidence、Market、Model/SHAP、Conflict/Recheck、Final Supervisor、Report/Trace。

`AVAILABLE / PARTIAL / UNAVAILABLE` 必须忠实来自 runtime contract；UI 不计算或伪造 Market/Model/Evidence。

## 8. Capability coverage

最终至少各有一份真实可审计 proof：

- core pipeline progress；
- text embellishment；
- related-party transaction；
- comparable IPO valuation；
- Evidence screenshot；
- single/batch report；
- API/UI；
- Dynamic New-IPO proof。

无 Existing Gold 的能力标记 `QUALITATIVE DEMONSTRATION`，不混入 M1/M2。

## 9. Freeze / one-shot Validation / package

只有 G2/G3/G4/G5 的核心行为冻结后才允许最终 Validation：

```text
freeze code / config / prompts / schemas / retriever / verifier / evaluator
freeze Market provider/schema identity
freeze model / feature / alert identity
record hashes
→ ALL19 Validation one shot
→ no Validation-driven retuning
→ latest-main full CI
→ Blind / PIT / provenance / determinism / security / licensing / path audits
→ artifact index
→ fresh clone
→ secure ZIP + SHA-256 manifest
```

## 10. 不可放松的治理

- Existing Gold immutable；
- `UNJUDGED != negative`；
- Gold 不进入 runtime；
- Validation one-shot；
- Blind 不用于优化；
- 无公司/股票/case/page/Gold-text 特判；
- LLM 不 invent Evidence / market fact；
- exact numeric claim 由 deterministic Calculation 支撑；
- Market PIT-safe；missing != zero；
- model score != probability；
- fallback != real-provider success；
- Secret/PDF/raw licensed EOD/raw journal/absolute path 不进 Git/bundle。

## 11. Competition Ready

只有：

```text
ALL79 M1 >=80%
+ ALL79 M2 >=85%
+ M3 =100%
+ Dynamic Market-X acceptable
+ formal D decision + Dynamic Model / SHAP acceptable
+ final frontend complete
+ capability proofs complete
+ one-shot Validation complete under freeze
+ latest-main CI / Blind / provenance / determinism / security / licensing PASS
+ fresh clone PASS
+ secure package PASS
= COMPETITION_READY
```

任何 fixed10 gain、final-three UI green 或 replay PASS 都不能单独触发 `COMPETITION_READY`。
