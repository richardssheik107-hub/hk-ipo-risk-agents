# Release Acceptance — Competition Closure

> Package checkpoint：`v0.4.0`
>
> Competition runtime：`v0.4.5`
>
> Role-B diagnostic track：`v0.4.6`
>
> Metric protocol：`v045_competition_metric_protocol_v2_existing_gold_only`
>
> 状态日期：`2026-08-28`
>
> 当前结论：**NOT COMPETITION_READY**

本文档是唯一实时 Gate 状态源。统一执行计划见 `COMPETITION_CLOSURE_PLAN.md`。

## 1. 当前实测快照

### B — M1/M2

```text
fixed-10 iter_004 = 10/10 real-LLM
M1 = 23.33%
M2 = 18.75%
dominant final symptom = semantic_extraction_miss
Validation opened = false
Blind outcome accessed = false
```

v0.4.6 已实现 offline / shadow / gated、structured smoke、LLM journal、Financial high-recall adapter、retrieval/risk waterfall 和 monotonicity；尚缺一份完整、同身份的 fixed-10 实测与逐单元根因矩阵。

### D — 上市后业务验证

已记录：

```text
2024 Validation IPOs = 70
1D / 5D / 20D / 60D artifacts = materialized
four canonical files + hashes = recorded
hash-bound receipt / CI validator = PASS
Blind outcome accessed = false
```

五日描述性结果：

| Metric | Value |
|---|---:|
| Precision | 0.3333 |
| Recall | 0.0435 |
| F1 | 0.0769 |
| PR-AUC | 0.3364 |
| ROC-AUC | 0.4246 |
| Top-10% hit rate | 0.4286 |
| Top-20% hit rate | 0.2857 |
| Base prevalence | 0.3286 |

结论：正式 artifact 与复现证据存在，但当前结果不足以支持“预测效果好”的强业务主张。D 仍需 current-main strict revalidation、final-three handoff，并由业务负责人完成改进或限定性解释。

### C / E / Product

```text
offline E2E / PDF integrity = 3/3
M3 traceability = 3/3 exactly 1.0
E real-provider accepted = 2/3
C strict observation contract = 1/3
M4 human reviews = 0/6
Evidence precise screenshot pipeline = open
competition capability cases = incomplete
```

## 2. Gate 总表

| Gate | 状态 | 关闭条件 |
|---|---|---|
| G0 Runtime / contracts | PASS implementation | CI 持续绿色、无回归 |
| G1 B forensic baseline | OPEN / P0 | 完整 v0.4.6 fixed-10、>=90% 根因覆盖 |
| G2 B M1/M2 | OPEN / P0 | ALL 79 Development M1>=0.80、M2>=0.85 |
| G3 D business value | PARTIAL / P0 | strict revalidation + final-three +可信业务结论 |
| G4 C/E final-three | OPEN / P1 | C 3/3、E accepted 3/3、M3=1.0 |
| G5 M4 | OPEN / P1 | 3 案 × 2 真人评审通过 |
| G6 Competition coverage / product | OPEN / P1 | 管线、粉饰、关联、估值、截图、报告/API/UI |
| G7 Freeze / Validation / package | OPEN / P1 | one-shot Validation + audits + secure bundle |

## 3. 已关闭的基础能力

- typed competition runtime sidecars；
- Existing Expert Gold inventory / Metric-v2 evaluator；
- 3 个真实 PDF offline E2E 与 integrity；
- governed MarketContext、IPOHeatSkill、MarketRegimeSkill；
- deterministic conflict detection 与 bounded re-check；
- Final Supervisor implementation 与 scope guard；
- Agent / Tool / Evidence trace implementation；
- Human Review 与 Streamlit workspaces；
- submission readiness、artifact index、audit、packager implementation；
- Role-D builder、strict checker、receipt、label-free handoff implementation。

“PASS implementation”不能替代最终 runtime evidence。

## 4. G1 — B forensic baseline

必须输出：

```text
parser preservation
retrieval candidate / rank / consumed
LLM transport / schema / scope / abstain
builder / normalization / reconciliation
verifier / final bucket
Evidence retention / page / anchor
risk and evidence root-cause matrix
```

关闭条件：

- offline / shadow / gated 同身份；
- shadow canonical result 等于 offline；
- gated 无额外网络调用；
- 至少 90% Risk/Evidence Unit 有 `PROVEN` earliest failure；
- 第一优先修复项可量化。

## 5. G2 — M1/M2

正式 Development Gate：

```text
case_count = 79
M1 >=0.80（target >=0.85）
M2 >=0.85（target >=0.88）
real_llm_cases = 79
new_manual_annotations_added = false
existing_gold_modified = false
Validation=false
2025 Blind input/outcome not used for optimization
```

fixed-10 只用于快速诊断，不作为正式比赛 PASS。

## 6. G3 — D business value

发布前：

1. 恢复 SHA 匹配的 frozen PR-E / PR-F runtime 与授权 EOD；
2. current-main build / strict checker PASS；
3. resume 和 fresh-directory byte-identical；
4. 生成 2410 / 2460 / 1318 label-free package；
5. 对 5D 弱表现提供经过治理的改进或诚实的业务边界说明。

赛题未给绝对 5D 及格线，因此不得伪造“官方 PASS”；但业务参考价值仍是比赛实质评价项。

## 7. G4 / G5 — C/E/M3/M4

最终必须：

```text
2410 / 2460 / 1318
real provider accepted = 3/3
Market strict contract = 3/3
scope PASS
severity floor preserved
M3 =1.0
M4 = 6 independent human reviews
```

fallback、mock 或 unavailable 不计 real-provider accepted。

## 8. G6 — 赛题覆盖与产品

必须补齐：

- 核心管线进度；
- 文本粉饰度原文切片；
- 关联交易；
- 同行估值比对 Skill 或等价可审计能力；
- Evidence bbox / 精确高亮截图；
- 单家/批量报告与 API/UI 人机复核；
- 三个典型案例及离线演示备份。

无 Existing Gold 的能力以 qualitative demonstration 报告，不篡改 M1/M2 分母。

## 9. G7 — Freeze / Validation / package

- B 代码、Prompt、Retriever、Schema、Verifier、Evaluator 冻结；
- one-shot ALL 19 Validation；
- latest-main full CI；
- Blind、provenance、determinism、security actual PASS；
- prediction table、Trace、Evidence、cases、release note、artifact index；
- secure ZIP 与 SHA-256 manifest。

## 10. 治理边界

继续保留：Gold immutable、Validation one-shot、Blind 隔离、Evidence scope、PIT、Calculation、Trace、Secret/PDF/raw data 安全。

已取消：固定迭代轮数、Runner-only、只读两个 summary、绝对禁止 Retriever/model/transport Development 实验。

## 11. Final checklist

```text
[x] runtime contracts / core implementation
[x] Existing-Gold audit / evaluator
[x] 3 real PDF offline E2E
[x] M3 measured 3/3 =1.0
[x] D four-file materialization recorded
[x] D receipt / validator / CI

[ ] v0.4.6 B forensic baseline complete
[ ] ALL 79 Development
[ ] M1 >=80%
[ ] M2 >=85%
[ ] D strict revalidation / determinism / final-three
[ ] D credible business-value conclusion
[ ] C strict final-three 3/3
[ ] E real-provider accepted 3/3
[ ] M4 6 human reviews
[ ] competition capability cases complete
[ ] Evidence screenshots complete
[ ] one-shot Validation complete under freeze
[ ] final CI / audits / package PASS
```
