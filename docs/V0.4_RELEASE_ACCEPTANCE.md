# v0.4 Release Acceptance — Competition Closure

> Package checkpoint: `v0.4.0`
>
> Competition runtime: `v0.4.5`
>
> Target end state: `v0.4.5 COMPETITION_READY`
>
> Current verdict: **NOT YET COMPETITION_READY**

本文件是当前比赛 Gate 与 blocker 的**唯一状态源**。历史 completion report 中的旧 Gate 文本不覆盖本文件。

## 1. 已关闭 Gate / tooling

| Gate | Status | Current evidence |
|---|---|---|
| Public competition runtime contracts | PASS | `competition_runtime_v1` sidecar 已进入代码与 CI |
| Remote LLM provider boundary | PASS | `openai_compatible` / `openai_responses`、structured validation、retry、trace metadata |
| Legal / Business LLM implementation | PASS implementation | real provider 路径已被 2410.HK 触发并完成结构化边界 hardening |
| Governed MarketContext | PASS | PR-B Core 继续 PIT governed，missingness 不补零 |
| Market Intelligence implementation | PASS | IPOHeatSkill / MarketRegimeSkill / bounded Market LLM |
| Market Intelligence AI runtime wiring | PASS | v0.4 AI runtime 已正式接入 |
| Market real-provider path | PASS implementation evidence | 两只真实 IPO 已通过 Volcengine/OpenAI-compatible Market interpretation |
| Core-only Market missingness | PASS | absent optional source 诚实降级，不再 crash |
| Conflict detection | PASS | deterministic, typed, auditable |
| Targeted re-check | PASS | 每个 conflict 最多一次，总预算受控 |
| LLM Final Supervisor implementation | PASS implementation | scope validation + severity floor + deterministic fallback |
| Agent / Tool / Evidence trace implementation | PASS | traceability 是度量值，不是硬编码 |
| Human Review | PASS | sidecar，不修改机器结论 |
| Streamlit competition product | PASS implementation | 五工作区可运行 |
| 3 real prospectus offline E2E matrix | **PASS** | 2410 / 2460 / 1318 全部 completed |
| 3-case PDF integrity | **PASS** | SHA-256 + byte size + physical pages = 3/3 |
| 3-case offline traceability | **PASS = 1.0** | 三个案例均为 1.0 |
| E submission artifact renderer | PASS implementation | 每案 reasoning log / case report / Gate-E1 evidence 由真实 run 渲染，不补写其他 lane |
| E Gate-E1 machine evidence | PASS implementation | accepted / provider_not_configured / provider_call_failed / rejected_out_of_scope 分离，mock/fallback 不算通过 |
| A submission readiness engine | PASS implementation | B/C/D/E/A handoff fail-closed 检查，missing 不推断为 PASS |
| A Blind / provenance / determinism audit tooling | PASS implementation | 输出独立 JSON，remote LLM 不做 byte-for-byte determinism 虚假承诺 |
| A artifact index / Runbook / packager | PASS implementation | SHA-256 index + `SUBMISSION_RUNBOOK.md` + COMPETITION_READY-only allowlist ZIP |
| 2025 Blind protection | PASS for current measured runs | 当前实测记录均为未访问 |

以上“PASS implementation”只说明工具已经实现，不代表其依赖的最终真实输入已经通过。

## 2. 三案例真实 offline E2E 结果

最终离线矩阵已经关闭“真实案例数量不足”这一工程 blocker：

```text
case            code      pages  status     conflicts  re-checks  trace  traceability
ipo_2024_02410  2410.HK     706  completed          6          3     22           1.0
ipo_2024_02460  2460.HK     579  completed          7          3     23           1.0
ipo_2024_01318  1318.HK     617  completed          7          3     23           1.0
```

附加事实：

```text
executed / declared              3 / 3
SHA-256 + size + page verified   3 / 3
structured workflow errors       0
outcome labels accessed          false
2025 Blind y accessed            false
```

这三个 case 可以作为工程稳定性 / trace 案例；但 2460 与 1318 的 offline 文档通道没有形成正式风险项，因此**不能把“E2E 跑通”误写成“Document quality 达标”**。

PR #135 之后，每案还可以生成：

```text
agent_reasoning_log.json / .md
case_report.md
gate_e1_evidence.json
```

这些工件只渲染本次 run 的真实 trace / channel / Evidence / conflict / re-check，不补写不存在的事实。

## 3. 当前开放 hard Gate

### Gate B1 — Real-LLM Document benchmark：OPEN / P0

Role B 当前正式测得的是 10 个 2020–2023 Development 真实 PDF 的 governed **offline** baseline：

```text
PDF found / SHA / page / analyzed      10 / 10 / 10 / 10
Risk Precision                         0.0%
Risk Recall                            0.0%
Risk F1                                0.0%
Evidence Recall@1                      20.0%
Evidence Recall@3                      20.0%
Evidence Recall@5                      20.0%
Physical-page correctness              100.0%
Real LLM cases                         0
```

因此：

- 赛题风险抽取目标没有被当前 benchmark 证明；
- Evidence Recall 目标没有被当前 benchmark 证明；
- 当前结果只能称 **offline governed baseline**，不能称 real-LLM 效果；
- 下一步必须使用相同固定 Development benchmark 运行真实 provider；
- real-LLM measurement 之后才允许做 Development-only targeted remediation；
- 2024 Validation 不得拿来 post-hoc 调参。

重点 failure mode 仍是：

```text
retrieval 有 Evidence
→ structured extraction 未形成有效 candidate
→ RiskItem / Verifier 没闭合
```

A 的 final readiness engine 会直接读取 B evaluator 的 `real_llm_cases / external_llm_called / risk_target / evidence_target / Blind flag`，不会把 demo 成功当作指标通过。

### Gate C1 — Final-matrix Market validation：OPEN / P1

C 的 implementation 已基本关闭。最终仍需在 submission 使用的同一 3-case run 中证明：

```text
market channel state explicit
+ Market trace event present
+ 每个 market event 有 governed namespaced input / Calculation / explicit no-Evidence reason
```

`available` 与诚实的 governed missing 都可以存在；禁止 zero/proxy 伪补。A 的 readiness engine 已实现该 trace accounting，但需要最终 AI matrix 工件才能给 PASS。

### Gate D1 — Multi-horizon outcome / evaluation package：OPEN / P0

比赛最终需要可复现的最小多周期结果：

```text
return_1d
return_5d
return_20d
return_60d
```

以及至少：

```text
test_predictions.csv
multi_horizon_results.csv
evaluation_summary.json
ai_vs_offline_report.json
```

仓库已有 multi-horizon foundation，但 final competition handoff 尚未进入 main/最终 reports。冻结 PR-C 5D 研究政策和既有结果不得被静默改写。

A readiness 会检查 horizon columns、非空结果和 `evaluation_summary.json` 的显式 2025 Blind=false；不会替 D 生成结果。

### Gate E1 — Real-provider Final Supervisor on final matrix：OPEN / P1

E 的实现、三案例 offline matrix、reasoning log / case report / machine Gate-E1 evidence 都已经关闭。

最新已测 offline 结果仍是：

```text
declared / with Gate evidence             3 / 3
cases_with_successful_llm_arbitration      0
cases_on_deterministic_fallback             3
matrix gate_e1.satisfied                 false
```

原因是该次验收环境没有成功的真实远端 provider synthesis。deterministic fallback 是正确降级，但**不能计为 successful LLM arbitration**。

最终必须用同一 final 3-case matrix：

```text
real provider
+ outcome = accepted
+ provider/model/prompt/request/hash/latency retained
+ scope check passed
+ no out-of-scope Evidence/Risk/Conflict reference
+ deterministic severity floor respected
```

只有每案与 matrix-level `gate_e1.satisfied=true` 才关闭 E1。

### Gate A1 — Final integration / submission freeze：TOOLING CLOSED / EXECUTION OPEN / P1

A 已经完成可提前完成的收口工具：

- `scripts/build_v045_submission_readiness.py`；
- `blind_audit.json` builder；
- `provenance_audit.json` builder；
- `determinism_audit.json` builder；
- `artifact_index.json` builder；
- `docs/SUBMISSION_RUNBOOK.md`；
- `scripts/package_v045_submission.py`；
- CI fail-closed smoke。

最终 execution 仍必须等 B1/D1/E1/C1 的真实 handoff 后完成：

- latest-main full CI；
- final 3-case AI smoke；
- Blind / provenance / determinism audit 实际 PASS；
- artifact completeness；
- `ai_vs_offline_report.json`；
- release note；
- submission archive；
- artifact index；
- 只有全部 hard Gate 实测通过后才标记 `COMPETITION_READY`。

Packager 不允许“先打包再解释”：`submission_readiness.json.competition_ready != true` 时直接拒绝。

## 4. Quality gap：Evidence bbox upstream grounding

当前 Evidence Viewer 可以按真实物理页渲染证据页，但 parser 尚不生成 `DocumentChunk.bbox`。

已测 2410.HK：

```text
parsed chunks              706
chunks with page           706
chunks with bbox             0
risk Evidence                2
risk Evidence with page      2
risk Evidence with bbox      0
```

UI 的降级是诚实的：没有 bbox 就不画框，不会自己猜坐标。

该项属于 **B（Evidence grounding）+ A（frozen provenance impact）**。因为增加 bbox 会改变 Evidence 内容/hash，所以不能由 UI 层修补，也不能无审计地回写 frozen PR-G/PR-H artifact。

当前赛题硬指标首先要求关键 Evidence 与可追溯来源；page grounding 已成立。若最终展示需要精确截图高亮，可在明确 versioning/provenance 后补齐 bbox，但它不得阻塞 B1/D1/E1 这些更高优先级 hard Gate。

## 5. Historical PR-H 与比赛 Gate 分离

历史 PR-H 继续保留 frozen closure 语义：原始 frozen PR-F runtime 或 hash-bound sanitized handoff 若不可恢复，Model Channel 就不能伪造为 available。

当前变化：

- “至少 3 个真实 PDF”已经满足；
- authentic frozen PR-F per-case runtime/handoff 仍未恢复；
- historical PR-H 不应被重新写成 COMPLETE；
- competition runtime 可以诚实地以 `Model Channel = unavailable` 提交，不需要为了 UI 重训模型。

## 6. 明确不作为当前 blocker 的事项

以下项目除非团队主动扩 scope，否则不阻止诚实提交：

- PIT-safe historical industry return，在 authoritative mapping 不存在时继续 missing；
- `ComparableIPOSkill`；
- broad new model search / hyperparameter tuning；
- full 438-case LLM run；
- 大规模 Retriever 重构；
- 为 PR-F 做替代训练；
- 仅为视觉效果的 UI 装饰；
- 无法证明 PIT 的代理市场数据。

## 7. 五人当前状态

| Role | Current status | Closure responsibility |
|---|---|---|
| A | **freeze tooling closed; final execution pending** | review/merge final handoffs, CI, audits, artifact index, package, release |
| B | **quality blocker** | real-LLM fixed benchmark, Risk/Evidence remediation, optional bbox grounding |
| C | implementation largely closed | final-matrix Market trace/missingness validation |
| D | **delivery blocker** | multi-horizon outputs + final evaluation artifacts + authentic model signal if available |
| E | **submission artifact tooling closed; real-provider acceptance pending** | final 3-case accepted LLM arbitration + final UI/case smoke |

## 8. 最终 checklist

```text
[x] competition runtime contracts
[x] Market Intelligence implementation
[x] Market AI runtime wiring
[x] Conflict / bounded re-check
[x] Trace / Human Review / Streamlit
[x] >=3 real PDF offline E2E cases
[x] 3-case measured traceability = 1.0
[x] E reasoning logs / case reports / machine Gate-E1 evidence generation
[x] A submission readiness / audit / artifact-index tooling
[x] reproducible Submission Runbook
[x] fail-closed submission packager implementation
[ ] fixed Development real-LLM Document benchmark produced
[ ] Risk extraction target materially demonstrated
[ ] Evidence Recall target materially demonstrated
[ ] final 3-case Market trace accounting PASS
[ ] 1D / 5D / 20D / 60D final artifact produced
[ ] test_predictions.csv produced
[ ] multi_horizon_results.csv produced
[ ] evaluation_summary.json produced
[ ] ai_vs_offline_report.json produced
[ ] real-provider Final Supervisor accepted on final matrix
[ ] final main CI green after all handoffs
[ ] blind / provenance / determinism audit actual PASS
[ ] final artifact index generated from real handoffs
[ ] submission bundle generated and security audit PASS
[ ] release note / COMPETITION_READY freeze complete
```

只有上面的 hard open Gate 被真实数据关闭后，才允许使用 `v0.4.5 COMPETITION_READY`。
