# v0.4 Release Acceptance — Competition Closure

> Package checkpoint: `v0.4.0`
>
> Competition runtime: `v0.4.5`
>
> Metric protocol: `v045_competition_metric_protocol_v2_existing_gold_only`
>
> Target end state: `v0.4.5 COMPETITION_READY`
>
> Current verdict: **NOT YET COMPETITION_READY**

本文件是当前比赛 Gate 与 blocker 的唯一状态源。最终指标定义以 `COMPETITION_METRIC_PROTOCOL.md` 为准；历史 completion report 中的旧 Gate/旧 metric 文本不覆盖本文件。

## 1. 已关闭 Gate / tooling

| Gate | Status | Current evidence |
|---|---|---|
| Public competition runtime contracts | PASS | `competition_runtime_v1` sidecar 已进入代码与 CI |
| Remote LLM provider boundary | PASS | `openai_responses` structured path 已由 1167.HK 真实 PDF 全流程验证 |
| Legal / Business LLM implementation | PASS implementation | real provider path 已被真实案例触发并 harden |
| Governed MarketContext | PASS | PR-B Core PIT governed，missingness 不补零 |
| Market Intelligence | PASS implementation | IPOHeatSkill / MarketRegimeSkill / bounded Market LLM |
| Conflict / bounded re-check | PASS implementation | typed + auditable + bounded |
| Agent / Tool / Evidence trace | PASS implementation | measured，不硬编码 |
| Human Review / Streamlit | PASS implementation | 五工作区可运行 |
| 3 real prospectus offline E2E | PASS | 2410 / 2460 / 1318 completed |
| 3-case PDF integrity | PASS | SHA-256 + size + physical pages = 3/3 |
| 3-case offline traceability | PASS = 1.0 | 三案均 1.0 |
| Single-case real-provider smoke | PASS | 1167.HK completed；Final Supervisor accepted；no deterministic fallback；scope PASS |
| E submission artifacts | PASS implementation | reasoning log / case report / Gate-E1 evidence |
| A submission readiness / audits / artifact index / packager | PASS implementation | fail-closed tooling 已实现 |
| Existing Expert Gold inventory | FROZEN | 101 inventory / 100 valid / 98 official materialized |
| Metric Protocol v2 Existing-Gold-Only | **FROZEN** | no new annotation / no UNJUDGED-as-negative |
| Existing-Gold coverage audit / evaluator | **PASS** | 79 Development / 19 Validation；128 primary positive Risk Units；217 primary Evidence Units |
| Fixed-10 Development iteration tooling | **PASS implementation** | deterministic subset + manifest binding + resume + evaluator + compact failure focus |
| 2025 Blind protection | PASS for current measured runs | 当前实测均未访问 |

以上 “PASS implementation” 不代表依赖的最终真实 metric handoff 已通过。

## 2. Metric-v2 正式比赛口径

### M1 Risk Extraction

```text
Primary:
Existing-Gold Official-aligned Accuracy
= correct evaluable positive Existing-Gold Risk Units
  / all evaluable positive Existing-Gold Risk Units

Official pass >=0.80
Project target >=0.85
```

未被旧 Gold 明确判断的 risk unit = `UNJUDGED`，不当 negative、不进入分母。

必须披露：evaluable support、correct count、per-risk support / correct / recall。Precision / Macro F1 只有旧 Gold 本身提供足够 exhaustive positive/negative judgment 时报告；不得通过补标获得。

### M2 Evidence

```text
Primary:
Existing-Gold Evidence Coverage Recall
= covered evaluable existing Evidence Units
  / all evaluable existing Evidence Units

Official pass >=0.85
Project target >=0.88
```

Recall@1/@3/@5/@10/@20 只作 diagnostics；官方原文件没有指定 Top-K。

M2 只使用比赛收尾之前已经存在的 Evidence/page/span/table/anchor 标注。允许确定性 normalization / identity standardization / exact duplicate dedupe；不允许新人工 Evidence 或人工重新做 semantic Evidence Group。

### M3 / M4 / M5

```text
M3 Traceability =1.0

M4 Explanation Quality
保持现有 final product rubric；本次 M1/M2 scope freeze 不增加 Gold 标注工作

M5 Outcome
1D/5D/20D/60D complete
Primary significant_drop_5d = return_5d <= -0.10
```

## 3. 当前真实运行证据

### 3.1 三案例 offline E2E

```text
case            code      pages  status     conflicts  re-checks  traceability
ipo_2024_02410  2410.HK     706  completed          6          3          1.0
ipo_2024_02460  2460.HK     579  completed          7          3          1.0
ipo_2024_01318  1318.HK     617  completed          7          3          1.0
```

```text
executed / declared              3 / 3
SHA-256 + size + page verified   3 / 3
structured workflow errors       0
outcome labels accessed          false
2025 Blind y accessed            false
```

这证明 offline 工程稳定性和 trace，不证明 M1/M2/M5 达标，也不关闭最终 E1 real-provider 3-case Gate。

### 3.2 1167.HK real-provider smoke

`ipo_2020_01167` 已完成真实 PDF 全流程：

```text
provider = openai_responses
model = ark-code-latest
status = completed
Final Supervisor = available / accepted
Gate E1 for this smoke case = PASS
deterministic fallback = false
scope guard = PASS
Validation accessed = false
2025 Blind accessed = false
```

当前 runtime：

```text
llm_timeout_seconds = 300
llm_max_retries = 0
```

这证明真实 provider runtime 可用，但不是最终 E1 closure；最终仍要求 2410 / 2460 / 1318 三案例 3/3 real-provider accepted。

## 4. 当前 hard Gate

### Gate B0 — Existing-Gold coverage audit / manifest：PASS

已完成只读扫描并生成 Existing-Gold evaluable manifest。当前实测：

```text
manifest_hash:
fcd12d34fcc64853ed778d0026b1e2c943a863549cd1652c6ced7c6145214d1c

official_existing_gold_case_count = 98
evaluable_development_case_count = 79
evaluable_validation_case_count = 19
primary_positive_risk_unit_count = 128
primary_evidence_unit_count = 217
```

Primary support：

```text
cash_burn_pressure         16
customer_concentration     32
redemption_rights          39
supplier_concentration     41
related_party_transaction   0 -> NOT_EVALUABLE_FROM_EXISTING_GOLD
```

治理：

```text
new_manual_annotations_added = false
existing_gold_modified = false
blind_2025_outcome_accessed = false
```

Validation case count 19 只是 coverage audit 统计，不代表启动了 Validation evaluator。

### Gate B1 — M1 Existing-Gold Risk Extraction：OPEN / P0

当前历史旧 offline diagnostic 继续保留，但已不再作为正式 metric-v2 证据：

```text
Risk Precision  0.0%
Risk Recall     0.0%
Risk F1         0.0%
Real LLM cases  0
```

关闭 B1 需要：

```text
ALL 79 Development real-LLM benchmark
+ metric_protocol_version = v045_competition_metric_protocol_v2_existing_gold_only
+ source manifest/hash
+ official_aligned_accuracy >=0.80
+ per-risk support disclosed
+ new_manual_annotations_added=false
+ existing_gold_modified=false
+ Blind=false
```

内部目标 Accuracy >=0.85。

固定 10 家 runner 只用于 Development 快速迭代，不可声称正式 PASS。

### Gate B2 — M2 Existing-Gold Evidence Coverage：OPEN / P0

历史 offline diagnostic：

```text
Evidence Recall@1 = 20%
Evidence Recall@3 = 20%
Evidence Recall@5 = 20%
Physical-page correctness = 100%
```

这些继续保留原始事实，但不等于 M2 primary。

最终 M2：

```text
Existing-Gold Evidence Coverage Recall >=0.85 official
Project target >=0.88
```

同时报告：

```text
Candidate Retrieval Recall@20
Reranked Recall@10
Recall@1/@3/@5/@10/@20
```

Primary 不固定 Top-5；不新增 Evidence Gold。

### Gate C1 — Final-matrix Market validation：OPEN / P1

最终 3-case run 需证明 explicit market state + governed trace accounting + no fabricated market numbers。真实 missing 合法，zero/proxy 伪补不合法。

### Gate D1 — M5 Multi-horizon / 5D evaluation：OPEN / P0

必须物化：

```text
return_1d
return_5d
return_20d
return_60d

test_predictions.csv
multi_horizon_results.csv
evaluation_summary.json
ai_vs_offline_report.json
```

Primary：

```text
significant_drop_5d = (return_5d <= -0.10)
```

报告 Precision / Recall / F1 / PR-AUC / ROC-AUC / Top-10% / Top-20% hit rate / base prevalence。赛题没有绝对 5D 合格线，不因 Validation 结果反转/重训 frozen PR-F。

### Gate E1 — Real-provider Final Supervisor：OPEN / P1

1167.HK 单案例 real-provider smoke 已 PASS，但 final matrix Gate 仍未关闭。

最终必须：

```text
2410.HK / 2460.HK / 1318.HK
real provider
accepted
complete call trace
scope PASS
severity floor respected
3/3
```

### Gate E2 — Explanation Quality：OPEN / P1

沿用现有 explanation-quality 方案。本次 Existing-Gold-only M1/M2 调整不新增任何 E2 Gold 标注任务。

### Gate A1 — Metric-v2 final readiness：EXECUTION OPEN / P1

A 基础 tooling 已实现，但 readiness 需要切换到 metric-v2 handoff；legacy-only B bool 或 Recall@5 不得作为最终 PASS。

最终需完成：

- review/merge B/C/D/E metric-v2 handoff；
- latest-main full CI；
- final 3-case AI smoke；
- Blind / provenance / determinism actual PASS；
- final metric dashboard / artifact completeness；
- artifact index / release note / submission archive / security audit；
- 全部 hard Gate PASS 后才标记 `COMPETITION_READY`。

## 5. Existing-Gold 执行计划

不再有新的 20-case annotation target。

当前开发流程：

```text
Existing Expert Gold frozen
→ read-only coverage audit [DONE]
→ fixed 10 Development debug subset
→ real-LLM run
→ Existing-Gold evaluator
→ M1/M2 + failure taxonomy
→ dominant-failure Development-only fix
→ next iteration
→ periodic larger Development checkpoint
→ ALL 79 Development benchmark
→ freeze code/prompt/evaluator/manifest/runtime
→ one-shot ALL 19 Validation
```

固定 10 家命令：

```bash
python scripts/run_v045_role_b_iteration.py --subset-only
python scripts/run_v045_role_b_iteration.py --iteration auto
```

详细见 `docs/V045_ROLE_B_FIXED10_ITERATION_WORKFLOW.md`。

不得因 Validation 结果继续调优。

## 6. Quality gap：Evidence bbox

page grounding 已可用，bbox 仍 P2。只有不影响 P0/P1 时再处理。

## 7. Historical PR-H 与比赛 Gate 分离

historical PR-H authentic frozen PR-F runtime/handoff 若不可恢复，Model Channel 继续 `unavailable`。Competition 不要求重训替代模型。

## 8. 明确不作为当前 blocker / 不再做

- **任何新的 M1/M2 人工 Gold 标注或 Gold 扩样**；
- PIT-safe industry return 缺失时的历史补数据研究；
- ComparableIPOSkill；
- broad model search / hyperparameter sweep；
- full 438-case LLM run；
- 大规模 Retriever 重构；
- PR-F 替代训练；
- presentation-only UI expansion；
- 无 PIT 证据的 market proxy。

## 9. 五人当前状态

| Role | Current status | Closure responsibility |
|---|---|---|
| A | metric governance + submission tooling | Existing-Gold evaluator/manifest governance + final readiness |
| B | **P0 quality blocker** | fixed-10 Development optimization → ALL 79 Development M1/M2 closure |
| C | implementation largely closed | final-matrix governed Market trace |
| D | **P0 delivery/evaluation blocker** | M5 1D/5D/20D/60D + frozen 5D metrics |
| E | implementation largely closed | final 3-case E1 real-provider + E2 current explanation quality |

## 10. 最终 checklist

```text
[x] competition runtime contracts
[x] Market implementation / wiring
[x] conflict / re-check / Trace / Human Review / Streamlit
[x] 3 real PDF offline E2E
[x] 3-case offline traceability =1.0
[x] A submission tooling
[x] Existing Expert Gold inventory frozen
[x] Metric Protocol v2 Existing-Gold-Only frozen
[x] Existing-Gold coverage audit / manifest generated
[x] no new manual annotation / existing Gold unchanged proven
[x] single-case real-provider runtime smoke PASS
[x] fixed-10 Development iteration tooling implemented

[ ] fixed-10 real-LLM baseline produced
[ ] B ALL 79 Development benchmark produced
[ ] M1 Existing-Gold Accuracy >=80%
[ ] M2 Existing-Gold Evidence Coverage Recall >=85%
[ ] Recall@K diagnostics produced
[ ] C final Market trace PASS
[ ] D multi-horizon artifacts produced
[ ] E real-provider 3/3 accepted
[ ] E current explanation quality PASS
[ ] final real traceability =1.0
[ ] final main CI green
[ ] blind / provenance / determinism PASS
[ ] artifact index / submission bundle / security audit PASS
[ ] release / COMPETITION_READY freeze complete
```
