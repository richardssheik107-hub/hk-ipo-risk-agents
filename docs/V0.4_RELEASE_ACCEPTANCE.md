# v0.4 Release Acceptance — Competition Closure

> Package checkpoint: `v0.4.0`
>
> Competition runtime: `v0.4.5`
>
> Metric protocol: `v045_competition_metric_protocol_v2_existing_gold_only`
>
> Status date: `2026-08-28`
>
> Target end state: `v0.4.5 COMPETITION_READY`
>
> Current verdict: **NOT YET COMPETITION_READY**

本文件是当前比赛 Gate 与 blocker 的唯一状态源。指标定义以 `COMPETITION_METRIC_PROTOCOL.md` 为准；操作顺序以 `V045_CURRENT_EXECUTION_PLAN.md` 为准。

## 1. 当前执行快照

### Role B

```text
frozen fixed-10 iter_004 = 10/10 real-LLM completed
M1 = 23.33%
M2 = 18.75%
dominant failure = semantic_extraction_miss
Validation opened = false
2025 Blind accessed = false
```

该结果只是 Development debug baseline，不能替代 ALL 79 Development 正式 handoff。

### Role D

PR #141 在 2026-08-27 记录了授权数据上的正式 M5 物化：

```text
2024 Validation IPOs = 70
governed filtered EOD = 433,776 rows / 438 target IPOs
D1_multi_horizon_evaluation = PASS
blind_2025_y_accessed = false
deterministic --resume = PASS
```

正式 artifact 记录哈希：

```text
test_predictions.csv
8521dabe3f976e5c532f55fe1571294eb9555ae644a32d524233680af74fa93a

multi_horizon_results.csv
f2d3382f2618e3d328155e9a37e81cd01a156cfc0787c8bc42320237dbb56725

evaluation_summary.json
6d542b025e5a9c52285a80fcdde198282c389ebc55773b40b644ccf0b74f7a63

ai_vs_offline_report.json
3aab6fc39f75f1c350f92ab329df97c97ca48105235d906f5ef213731f180c94
```

该历史物化已写入哈希绑定机器凭据并进入 CI：

```text
reports/frozen/v045_role_d_m5_materialization_receipt.json
python scripts/validate_v045_role_d_receipt.py
```

Receipt 会绑定 frozen PR-E/PR-F manifest、Metric Protocol、四文件哈希和治理声明。它证明上述外部治理物化有可审计记录，但不替代拥有授权行情和完整 frozen runtime 时必须运行的 current-main strict checker。

Runtime、授权 EOD 与完整 PR-E/PR-F research runtime 按规则未提交。当前 `main` 已新增更严格的独立 acceptance checker，因此 A 最终打包前仍需在持有不可变输入的环境中进行一次 current-main strict revalidation，并物化 final-three D→E label-free package。

### Role C / E final matrix

```text
offline completed / PDF integrity = 3/3
real provider attempted = 3/3
E1 accepted = 2/3
E1 fallback = 1/3
M3 overall_traceability = 3/3 exactly 1.0
C explicit market state / trace = 3/3
C strict observation contract = 1/3
M4 human reviews = 0/6
```

2410 与 1318 的 Final Supervisor 为 accepted real-provider arbitration；2460 两次引用越界 `conflict_id`，被 scope guard 拒绝并 honest fallback。C 的 2460/1318 unavailable observations 仍缺完整 `unit` / `derivation` metadata。

## 2. 已关闭的实现与治理 Gate

| Gate | Status | Evidence |
|---|---|---|
| Public competition runtime contracts | PASS | typed sidecars + CI |
| Remote LLM provider boundary | PASS | 1167.HK real PDF smoke |
| Governed MarketContext | PASS implementation | PIT governed；missingness 不补零 |
| Conflict / bounded re-check | PASS implementation | typed、auditable、bounded |
| Agent / Tool / Evidence trace | PASS implementation | measured，不硬编码 |
| Human Review / Streamlit | PASS implementation | product workspaces available |
| 3 real prospectus offline E2E | PASS | 2410 / 2460 / 1318 |
| 3-case PDF integrity | PASS | SHA / size / physical pages 3/3 |
| 3-case offline traceability | PASS = 1.0 | 3/3 |
| Submission tooling | PASS implementation | readiness / audits / index / packager |
| Existing Expert Gold inventory | FROZEN | 101 / 100 / 98 |
| Metric Protocol v2 | FROZEN | Existing-Gold-only |
| Existing-Gold evaluator | PASS implementation | 79 Development / 19 Validation |
| Role-D governed M5 builder | PASS implementation | four canonical artifacts |
| Role-D strict acceptance checker | PASS implementation | exact four files / 70 cases / independent recomputation |
| PR-F label-free product handoff | PASS implementation | exact package / checksum / no labels |
| A Role-D four-file readiness contract | PASS implementation | includes `ai_vs_offline_report.json` |
| Role-D 70-case M5 materialization | **PASS RECORDED** | PR #141 external governed run |
| Role-D hash-bound receipt | **PASS / CI** | frozen bindings + artifact hashes + governance |

“PASS implementation” 不等于最终 runtime evidence 已存在于 Git；被忽略的 licensed/runtime inputs 必须在 release 环境中重新验证。Receipt 是历史物化的机器可验记录，也不替代 live strict revalidation。

## 3. Metric-v2 正式口径

### M1 Risk Extraction

```text
Existing-Gold Official-aligned Accuracy
= correct evaluable positive Existing-Gold Risk Units
  / all evaluable positive Existing-Gold Risk Units

Official pass >=0.80
Project target >=0.85
```

### M2 Evidence

```text
Existing-Gold Evidence Coverage Recall
= covered evaluable existing Evidence Units
  / all evaluable existing Evidence Units

Official pass >=0.85
Project target >=0.88
```

`UNJUDGED` 不当 negative；Recall@1/@3/@5/@10/@20 只作 diagnostics。

### M3 / M4 / M5

```text
M3 Traceability = 1.0
M4 Explanation Quality = current human-review rubric
M5 horizons = 1D / 5D / 20D / 60D
Primary significant_drop_5d = return_5d <= -0.10
```

赛题没有给出绝对 5D 模型合格线。不得因 Validation 指标反转 score、重训 PR-F、改 threshold 或把未校准 score 称为 probability。

## 4. Gate B0 — Existing-Gold coverage：PASS

```text
manifest_hash = fcd12d34fcc64853ed778d0026b1e2c943a863549cd1652c6ced7c6145214d1c
official_existing_gold_case_count = 98
evaluable_development_case_count = 79
evaluable_validation_case_count = 19
primary_positive_risk_unit_count = 128
primary_evidence_unit_count = 217
new_manual_annotations_added = false
existing_gold_modified = false
blind_2025_outcome_accessed = false
```

## 5. Gate B1 / B2 — M1/M2：OPEN / P0

当前 fixed-10 M1=23.33%、M2=18.75%，远低于目标。关闭顺序：

```text
iter_004 failure_focus
→ one dominant-failure Fixer
→ bounded fixed-10 rerun
→ larger Development checkpoint
→ ALL 79 Development real-LLM benchmark
→ freeze
→ one-shot ALL 19 Validation
```

正式关闭要求：

```text
ALL 79 Development
M1 >=0.80
M2 >=0.85
real_llm_cases = 79
new_manual_annotations_added = false
existing_gold_modified = false
Blind = false
Recall@1/@3/@5/@10/@20 complete
```

## 6. Gate D1 — M5：PASS RECORDED / RELEASE REVALIDATION REQUIRED

### 6.1 已完成并有记录的正式物化

```text
return_1d / return_5d / return_20d / return_60d
70 frozen 2024 Validation cases
four canonical artifacts
AI-vs-offline descriptive comparison
blind_2025_y_accessed = false
threshold_or_model_retuned_on_validation = false
deterministic resume = PASS
```

五日指标：

| Metric | Value |
|---|---:|
| Precision | 0.3333 |
| Recall | 0.0435 |
| F1 | 0.0769 |
| PR-AUC | 0.3364 |
| ROC-AUC | 0.4246 |
| Top-10% hit rate | 0.4286 |
| Top-20% hit rate | 0.2857 |
| Base prevalence | 0.3286 |

### 6.2 当前代码与机器证据已完成

```text
scripts/build_v045_role_d_m5.py
scripts/check_v045_role_d_m5.py
src/ipo_risk/evaluation/role_d_m5.py
src/ipo_risk/evaluation/role_d_acceptance.py
scripts/build_v04_pr_f_product_handoff.py
src/ipo_risk/modeling/pr_f_product_handoff.py
reports/frozen/v045_role_d_m5_materialization_receipt.json
scripts/validate_v045_role_d_receipt.py
src/ipo_risk/evaluation/role_d_receipt.py
docs/V045_ROLE_D_FINAL_CLOSURE.md
```

严格 live checker 会独立验证：

- canonical 目录恰好四个文件；
- frozen metric protocol；
- frozen PR-E / PR-F runtime identity；
- governed EOD；
- exact 70-case 2024 Validation set；
- 1/5/20/60 trading-session returns；
- 5D label / score / threshold / SHAP；
- 五日指标独立重算；
- AI-vs-offline consistency；
- provenance 与 absolute-path safety。

网络无关 receipt validator 会验证历史记录与当前 frozen manifests/metric protocol 没有漂移，并对缺第四文件、Blind/调参标志、哈希漂移、本地绝对路径和 secret-like value fail closed。

### 6.3 A 打包前仍需完成

```text
1. 恢复 SHA 完全匹配的完整 frozen PR-E runtime
2. 恢复 SHA 完全匹配的完整 frozen PR-F runtime
3. 恢复授权 EOD 或合法 governed cache
4. 在 current main 重新 build Role-D four-file handoff
5. strict checker verdict = PASS
6. --resume byte-identical
7. fresh-directory byte-identical
8. 直接读取 configs/v045_demo_cases.json 生成 final-three label-free D→E package
9. product package validator PASS
```

这些是不可变输入驱动的 release evidence，不是新的模型研发任务。缺输入时必须 fail closed，不得重训、替代行情或伪造 package。

### 6.4 v2 candidate 边界

Role-D v2 high-recall output 仍是 `RESEARCH CANDIDATE / A REVIEW REQUIRED`。在正式治理决议前不得替换 frozen PR-F，也不得用于改写上述 D1 历史指标。

## 7. Gate C1 — Final-matrix Market validation：OPEN / P1

需完成 3/3 strict observation metadata、explicit state、governed trace 和 no fabricated market numbers。真实 missing 合法；zero/proxy 伪补不合法。

## 8. Gate E1 — Real-provider Final Supervisor：OPEN / P1

最终必须：

```text
2410.HK / 2460.HK / 1318.HK
real provider
accepted
complete call trace
scope PASS
severity floor respected
3/3
```

当前为 2/3 accepted；2460 honest fallback 不计成功 arbitration。

## 9. Gate E2 — Explanation Quality：OPEN / P1

每案至少两名独立 human reviewer，当前为 0/6。LLM review 不能代替正式人类评分。

## 10. Gate A1 — Final readiness：OPEN / P1

最终需完成：

- B ALL 79 M1/M2；
- D current-main strict revalidation + final-three product package；
- C1 3/3；
- E1 3/3；
- M4 human review；
- latest-main full CI；
- Blind / provenance / determinism actual PASS；
- artifact index / release note / security audit / submission archive。

A packager 必须继续拒绝未就绪 bundle。

## 11. 五人当前状态

| Role | Current status | Closure responsibility |
|---|---|---|
| A | governance + submission tooling | final integration / CI / package |
| B | **P0 quality blocker** | targeted Development loop → ALL 79 |
| C | **strict C1 1/3** | complete governed unavailable metadata |
| D | **M5 PASS recorded；receipt/CI PASS；runtime-local release revalidation pending** | strict current-main rerun + final-three label-free package |
| E | **E1 2/3；M3 PASS；M4 0/6** | scope-clean 3/3 + human review |

## 12. 最终 checklist

```text
[x] competition runtime contracts
[x] Existing Expert Gold / Metric-v2 frozen
[x] Existing-Gold evaluator implementation
[x] 3 real PDF offline E2E
[x] final real traceability = 1.0 for 3/3
[x] Role-D M5 builder / strict checker / product handoff implementation
[x] Role-D 70-case formal materialization recorded
[x] Role-D four canonical output hashes recorded
[x] Role-D deterministic resume recorded PASS
[x] Role-D hash-bound receipt validated against frozen manifests
[x] Role-D receipt validator runs in CI

[ ] B ALL 79 Development benchmark
[ ] M1 >=80%
[ ] M2 >=85%
[ ] C final Market strict validation 3/3
[ ] D current-main strict revalidation PASS
[ ] D fresh-directory determinism PASS
[ ] D→E final-three label-free package materialized and validated
[ ] E real-provider 3/3 accepted
[ ] M4 human review PASS
[ ] latest-main CI green
[ ] Blind / provenance / determinism final audits PASS
[ ] artifact index / submission bundle / security audit PASS
[ ] release / COMPETITION_READY freeze complete
```
