# 2410.HK legacy compatibility packet

Legacy Case: `real_case_001`

This directory is retained for audit compatibility and is not assigned in
`expert_golden_100_v1`. The formal catalog-backed task is
`case_packets/ipo_2024_02410/`. Do not use the historical first GPT result for the
new formal blind pass.

For a formal annotation, use the `ipo_2024_02410` packet, locally obtained original
PDF, Protocol v1.1, common instructions, and frozen Primary Prompt in one fresh
ChatGPT conversation.

Verify the PDF against the SHA-256 and page count in `case_metadata.json`. This directory intentionally contains no PDF, local path, Human Golden, prior GPT answer, Retriever output or Agent output.
