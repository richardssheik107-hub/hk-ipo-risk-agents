# 2517.HK blind packet

Case: `ipo_2023_02517`

Upload the locally obtained original PDF, the root Protocol v1.1, common annotation instructions, and `blank_annotation.json` to one fresh ChatGPT conversation.

Verify the PDF against the SHA-256 and page count in `case_metadata.json`. This directory intentionally contains no PDF, local path, Human Golden, prior GPT answer, Retriever output or Agent output.
