# v0.2.0 发布验收记录

验收日期：2026-08-06

发布候选基线：`main@cdc3c69d9f638077593e73f08acc673b995ae1db`

目标版本：`v0.2.0-real-document-slice`

## 验收结论

v0.2.0 发布闸门通过，可以创建正式 Tag 与 GitHub Release。本次验收未修改公共 Schema、WorkflowState、IPOAnalysisService 或组件接口。

## 自动化与隔离环境复跑

从远程 `main` 新建独立克隆，并在 Python 3.12.10 全新虚拟环境中执行依赖安装和验收：

| 检查 | 结果 |
| --- | --- |
| `python -m pip install -e ".[dev]"` | 通过 |
| `pytest -q` | 284 passed |
| `python scripts/validate_project.py` | `completed`，verified=3，pending=1 |
| `python scripts/validate_competition_data.py` | 通过 |
| `python -m compileall -q app src` | 通过 |
| `python scripts/check_real_v02_e2e.py` | 通过 |

独立复跑使用全新克隆和虚拟环境，不复用当前工作区的可编辑安装。该记录证明仓库可从远程基线独立安装和运行；它不冒充另一位团队成员的人工操作记录。

## 2410.HK 第二次独立证据复核

复核对象：浙江同源康醫藥股份有限公司招股书，股票代码 2410.HK。

| 指标 | 主证据 | 复核结果 |
| --- | --- | --- |
| 经营活动现金流 | PDF物理页562 | 截至2024年3月31日止三个月，`(83,918)`人民币千元，标准化为`-83,918` |
| 期末现金及现金等价物 | PDF物理页563 | 截至2024年3月31日，`77,208`人民币千元 |
| 现金交叉验证 | PDF物理页616 | 现金及现金等价物`77,208`人民币千元 |
| 现金跑道 | 页562与563 | `77,208 / (83,918 / 3) = 2.76`个月 |

复核同时确认繁体中文、括号负数、逗号、币种、单位、期间和物理页码均被保留。Retriever对页563和页562均排名第1，页665和683的非财务误命中未进入结果。

## Streamlit人工界面验收

- 真实PDF场景：`status=completed`，706个非空页Chunk，解析错误0；
- Evidence页码：563与562；
- Calculation：2.76个月；
- Risk：verified，critical，规则分90；
- Predictor故障场景：`status=partial`，`prediction=None`，结构化错误和失败日志可见，页面未崩溃。

## 已知限制

- 当前真实闭环只覆盖现金跑道；Legal、Business和Market Agent在真实配置中为`unavailable`；
- ReportGenerator仍为Mock格式化实现，LLMProvider未进入生产链路；
- 规则分90不是上市后下跌概率，也不构成投资建议；
- 缺少真实市场情绪时明确降级，不使用Mock市场数据补分；
- 扫描型PDF/OCR、统计预测模型和多公司泛化仍属于后续版本范围；
- 赛事原始ZIP、Excel、大型CSV及本地复核图片不进入Git。

## 发布决定

发布候选通过。正式Tag应指向包含本验收记录和发布说明的冻结提交；如需回退，使用该Tag恢复。
